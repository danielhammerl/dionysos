Datentypen:

Datentypen:
uint8: unsigned integer 8 byte (nicht implementiert)
uint16: unsigned integer 16 byte
void: nichts (nicht implementiert)

Variablen:

Beispiel code für definieren einer variable:
uint16 x = 88;

Methoden: (nicht implementiert)

void xyz() {
    ...
}

uint16 xyz(uint16 param) {
    ...
    return param;
}

xyz();

Kommentare:
lines mit // zeichen
block mit /* ... */

Operatoren:
+ -

conditions: (nicht implementiert)

if(condition) {
    ...
} else if(condition) {

} else {

}